---
description: Her değişiklik sonrası otomatik yerel git commit
---

// turbo-all

Değişiklikleri stage'e al ve commit et:
```
cd a:\TOOLS\kodlama\km\eokul && git add . && git commit -m "{{COMMIT_MESSAGE}}"
```
